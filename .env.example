# Required only for the Chatbot tab in frontend/app.py.
# Without it, the app runs fully (upload, KPIs, trends, ranking) but the
# chatbot tab shows a disabled message instead of failing.
ANTHROPIC_API_KEY=
